// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Zmień na swój adres docelowy. GitHub Pages używa zazwyczaj formatu: https://<username>.github.io/<repo-name>/
  site: 'https://lukaszrosicki.github.io', 
  output: 'static',
});